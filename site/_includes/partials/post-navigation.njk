{% set contentItems = null %}
{% set contentType = "" %}

{% if page.url | getContentType === 'articles' %}
  {% set contentItems = collections.articles %}
{% endif %}

{% if page.url | getContentType === 'blog' %}
  {% set contentItems = collections.blogPosts %}
{% endif %}

{% set currentPostIndex = contentItems | indexOfPostByUrl(page.url) %}
{% set previousPost = contentItems[currentPostIndex - 1] %}
{% set nextPost = contentItems[currentPostIndex + 1] %}

<div class="post-navigation">
    <div class="post-navigation__prev">
      {% if previousPost %}
        <a href="{{ previousPost.url }}" class="post-navigation__link">{{ 'i18n.nav.post_nav.back' | i18n(locale) }}</a>
        <p><a href="{{ previousPost.url }}" class="post-navigation__title">{{previousPost.data.title}}</a></p>
      {% endif %}
   </div>
   <div class="post-navigation__next">
      {% if nextPost %}
        <a href="{{ nextPost.url }}" class="post-navigation__link">{{ 'i18n.nav.post_nav.next' | i18n(locale) }}</a>
        <p><a href="{{ nextPost.url }}" class="post-navigation__title">{{nextPost.data.title}}</a></p>
      {% endif %}
    </div>
</div>