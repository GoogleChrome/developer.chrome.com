{% extends "layouts/base.njk" %}

{% block title_bar %}
  {% include 'partials/breadcrumbs.njk' %}
{% endblock %}

{% block content %}
  <div class="display-flex">

    <article class="stack measure-long type center-margin pad-400">
      <h1>{{ title }}</h1>
      {{ content | safe }}

{% import 'macros/reference.njk' as macros with context %}

{# TODO(samthor): This currently generates a list of all known APIs, but we only
   render pages for specific documented APIs. #}

<h2>Stable APIs</h2>
{{ macros.renderReferenceTable(collections.types, 'stable') }}

<h2>Beta APIs</h2>
{{ macros.renderReferenceTable(collections.types, 'beta') }}

<h2>Dev APIs</h2>
{{ macros.renderReferenceTable(collections.types, 'dev') }}

    </article>
  </div>
{% endblock %}